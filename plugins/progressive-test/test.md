# Main Title (H1)

This is intro text with **bold**, *italic*, and `inline code`.

## Features Section (H2)

Here's what we support:

- Unordered list item one
- Item with `code` and **bold**
- Third item

## Code Examples (H2)

Regular code block:

```javascript
function hello() {
    return "world";
}
```

### Nested Heading (H3)

Ordered list:

1. First step
2. Second step with *emphasis*
3. Third step

#### Deep Heading (H4)

> This is a blockquote
> spanning multiple lines

##### Even Deeper (H5)

Indented code block in a list:

1. Copy the folder:
   ```bash
   cp -r source/ dest/
   ```

2. Run the build:
   ```sh
   npm run build
   ```

###### Smallest Heading (H6)

Final paragraph with [a link](https://example.com) and some **bold _nested_ formatting**.

## Conclusion (H2)

That's all the test cases!
